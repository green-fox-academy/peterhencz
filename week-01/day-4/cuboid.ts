'use strict'

let a: number = 5.0;
let b: number = 6.0;
let c: number = 3.0;
let surface: number = (((a * b) + (b * c) + (a * c)) * 2);
        console.log("Surface Area: " + surface);
let volume: number = (a * b * c);
        console.log("Volume Area: " + volume);

